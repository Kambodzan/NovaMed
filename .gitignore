
# Node
node_modules/
dist/
*.local

# Python
__pycache__/
*.py[cod]
.venv/
venv/
.pytest_cache/
.coverage
htmlcov/

# Środowisko
.env
.env.*
!.env.example
!.env.*.example

# IDE / OS
.idea/
.vscode/
Thumbs.db
.DS_Store

# Pliki robocze
*.log

# Załączniki telewizyt (dev)
backend/uploads/

# Certyfikaty dev (generowane przez scripts/make-cert.py)
certs/

# Lokalne notatki/sekrety (konta testowe, dane Supabase, klucze Twilio itp.)
external/

# kopie zapasowe bazy (backup-db.ps1)
backups/

# === Pomocnicze artefakty pracy nad dokumentacją/audytem (nie wpływają na kod) ===
# Pipeline generowania i audytu docx + rendery diagramów + dane pośrednie.
docs/_clone/
diagramy
docs/_chapters/
docs/AUDYT.md
docs/AUDYT_DOKUMENTACJI.md
# Skrypty narzędziowe do składania dokumentacji/diagramów (nie część aplikacji).
scripts/build-dokumentacja.py
scripts/render-diagramy.py
scripts/split-erd.py
